<%= link_to "Add a restaurant", new_restaurant_path, class: "btn btn-primary m-3"%>
<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%=restaurant.name%> <%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary m-3"%></li>
  <% end %>
</ul>
